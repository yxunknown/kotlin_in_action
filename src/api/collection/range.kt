package api.collection

fun main(args: Array<String>) {
    for (index in 0 until 2) {
        println(index)
    }
    println("-----------华丽の分割线----------")
    for (index in 1..2) {
        println(index)
    }
}